syntax="proto3";

service ProtoServiceDiscovery{
    rpc RegisterService(Register) returns (registration_result);
}

message Register{
    string service_type = 1;
    string address = 2;
    int32 inner_port = 3;
    int32 extern_port = 4;
}

message registration_result{
    int32 success = 1;
}